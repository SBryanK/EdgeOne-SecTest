[build]
builder = "NIXPACKS"

[deploy]
startCommand = "streamlit run pentestweb.py --server.port $PORT --server.address 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
